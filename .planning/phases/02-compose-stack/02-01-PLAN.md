---
phase: 02-compose-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .devcontainer/docker-compose.yml
  - .devcontainer/docker-compose.gpu.yml
  - .devcontainer/configure-ollama-endpoint.sh
  - dot-pi/models.json
  - dot-opencode/config.json
  - tests/container/test_configs.sh
  - tests/container/test_pi.sh
  - tests/container/test_opencode.sh
autonomous: true
requirements:
  - COMPOSE-01
  - COMPOSE-02
  - COMPOSE-03
  - COMPOSE-04
  - COMPOSE-05
must_haves:
  truths:
    - "A compose file in .devcontainer defines dev-env and ollama services on an explicit named bridge network"
    - "dev-env waits for ollama health before starting"
    - "The compose default routes AI tools to http://ollama:11434"
    - "Setting OLLAMA_HOST before compose startup rewrites the tool configs to use the host fallback"
    - "The base compose file keeps Ollama internal-only with no published host port"
    - "The compose artifacts are Docker Compose v2 and Podman friendly"
  artifacts:
    - path: ".devcontainer/docker-compose.yml"
      provides: "Base two-service compose stack"
      contains: "services:"
    - path: ".devcontainer/configure-ollama-endpoint.sh"
      provides: "Runtime config bridge from OLLAMA_HOST to Pi/OpenCode JSON configs"
      contains: "OLLAMA_HOST"
---

<objective>
Create the Phase 2 compose stack and the minimum supporting config updates so `docker compose up`
brings up `dev-env` and `ollama` on an internal network, with compose-first Ollama defaults and a
real host fallback override.

Output:
- `.devcontainer/docker-compose.yml`
- `.devcontainer/docker-compose.gpu.yml`
- `.devcontainer/configure-ollama-endpoint.sh`
- Updated Pi/OpenCode config defaults and matching container tests
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-compose-stack/02-CONTEXT.md

<interfaces>
D-01/D-02: Default tool endpoint is `http://ollama:11434`; `OLLAMA_HOST` may override it to the Windows host.
D-03: Use `${DEV_ENV_IMAGE:-...}` and `${OLLAMA_IMAGE:-...}` style image references.
D-04: Persist only `/root/.ollama` and `/home/dev/.cache` in Phase 2.
D-05/D-06: Base compose file publishes no host port and uses explicit network name `ai-net` plus `x-podman` metadata.
D-07: `depends_on: condition: service_healthy` gates `dev-env` startup.
D-08: Runtime script rewrites `~/.pi/agent/models.json` and `~/.opencode/config.json` based on `OLLAMA_HOST`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the compose-first stack and endpoint bridge</name>
  <files>
    .devcontainer/docker-compose.yml
    .devcontainer/docker-compose.gpu.yml
    .devcontainer/configure-ollama-endpoint.sh
    dot-pi/models.json
    dot-opencode/config.json
    tests/container/test_configs.sh
    tests/container/test_pi.sh
    tests/container/test_opencode.sh
  </files>
  <action>
    1. Create `.devcontainer/docker-compose.yml` with `dev-env` and `ollama` services, explicit `ai-net`, `x-podman`, named volumes, internal-only Ollama, and health-gated startup.
    2. Create `.devcontainer/docker-compose.gpu.yml` as an optional GPU overlay using `deploy.resources.reservations.devices`.
    3. Create `.devcontainer/configure-ollama-endpoint.sh` to rewrite Pi and OpenCode config URLs from `OLLAMA_HOST`, appending `/v1` as needed.
    4. Update checked-in Pi/OpenCode defaults to `http://ollama:11434/v1`.
    5. Update container tests to expect the compose-first default and continue rejecting localhost/127.0.0.1.
  </action>
  <verify>
    <automated>
      test -f .devcontainer/docker-compose.yml
      test -f .devcontainer/docker-compose.gpu.yml
      test -f .devcontainer/configure-ollama-endpoint.sh
      grep -q 'OLLAMA_HOST=http://ollama:11434' .devcontainer/docker-compose.yml
      grep -q 'condition: service_healthy' .devcontainer/docker-compose.yml
      grep -q 'name: ai-net' .devcontainer/docker-compose.yml
      ! grep -q '^\s*ports:' .devcontainer/docker-compose.yml
      grep -q 'ollama:11434/v1' dot-pi/models.json
      grep -q 'ollama:11434/v1' dot-opencode/config.json
      docker compose -f .devcontainer/docker-compose.yml config >/dev/null
    </automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `docker compose -f .devcontainer/docker-compose.yml config` exits 0
- Base compose file defines both services, named volumes, and the explicit `ai-net` network
- Pi and OpenCode defaults point to the compose-internal Ollama hostname
- Host fallback remains available through `OLLAMA_HOST`
</success_criteria>

<output>
After completion, create `.planning/phases/02-compose-stack/02-01-SUMMARY.md`
</output>
