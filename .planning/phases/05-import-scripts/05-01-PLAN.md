---
phase: 05-import-scripts
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - image-import.sh
  - image-import.ps1
  - tests/container/test_import_scripts.sh
  - tests/container/run_all.sh
  - README.md
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
must_haves:
  truths:
    - "Import verifies the archive checksum before extraction or image load"
    - "Bash and PowerShell import scripts consume the same archive and payload layout produced in Phase 4"
    - "Images are restored from the bundled images.tar before compose validation runs"
    - "Compose validation targets the existing .devcontainer/docker-compose.yml contract"
    - "Bundled CUDA or driver installers run when present, and missing GPU installers produce a clear warning with cuda-prep guidance"
  artifacts:
    - path: "image-import.sh"
      provides: "Linux/WSL import workflow"
      contains: "docker load"
    - path: "image-import.ps1"
      provides: "Windows host import workflow"
      contains: "docker load"
---

<objective>
Create the import-side transport workflow so an air-gapped machine can verify a transferred archive,
restore the compose images, validate the compose stack, and apply bundled CUDA or driver installers
when they are included.

Output:
- `image-import.sh`
- `image-import.ps1`
- Minimal tests/docs needed to keep the import contract explicit and verifiable
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-import-scripts/05-CONTEXT.md
@.planning/phases/04-export-scripts-cuda-prep/04-01-SUMMARY.md

<interfaces>
D-01: Input is the Phase 4 artifact trio: `<bundle>.tar.gz`, `<bundle>-manifest.json`, and `<bundle>-SHA256SUMS`.
D-02: Verify SHA256 before extraction, then extract and load `<bundle>/images.tar` with `docker load`.
D-03: Validate `.devcontainer/docker-compose.yml` after images exist locally.
D-04: Report enough service/config status for the operator to confirm the restored environment is ready.
D-05: If `cuda/metadata.json` and installer files are present, install them; if GPU-oriented metadata exists but installers are absent, warn with an explicit `cuda-prep` rerun hint.
D-06: Keep the CPU-only path successful when no CUDA payload is bundled.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the archive verification and image restore workflow</name>
  <files>
    image-import.sh
    image-import.ps1
    tests/container/test_import_scripts.sh
    tests/container/run_all.sh
    README.md
  </files>
  <action>
    1. Implement Bash and PowerShell import scripts that accept the exported bundle path and verify its checksum before any extraction or `docker load`.
    2. Extract the archive to a temporary workspace, read the payload manifest, and restore the bundled images from `images.tar`.
    3. Run compose validation against `.devcontainer/docker-compose.yml` after import and report the resulting status clearly.
    4. Add minimal verification coverage for help text and the checksum/load/compose-validation contract, and wire it into the existing container test runner.
  </action>
  <verify>
    <automated>
      test -f image-import.sh
      test -f image-import.ps1
      grep -q 'docker load' image-import.sh
      grep -q 'SHA256' image-import.sh
      grep -q 'docker load' image-import.ps1
      test -f tests/container/test_import_scripts.sh
    </automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add optional CUDA and driver installation handling</name>
  <files>
    image-import.sh
    image-import.ps1
    README.md
  </files>
  <action>
    1. Detect whether the extracted archive contains a `cuda/` payload and use its metadata/checksum files to determine what can be installed.
    2. Execute the bundled installers when present, keeping Linux/WSL and Windows-host behavior explicit in the respective scripts.
    3. If GPU-oriented metadata exists but required installers are missing, emit a warning that includes the exact `cuda-prep` rerun guidance instead of failing silently.
  </action>
  <verify>
    <automated>
      grep -q 'cuda-prep' image-import.sh
      grep -q 'metadata.json' image-import.sh
      grep -q 'cuda-prep' image-import.ps1
      grep -q 'metadata.json' image-import.ps1
    </automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Import scripts exist for Bash and PowerShell and describe the same restore contract
- The import flow fails before image load on checksum mismatch
- The restored images can be validated against `.devcontainer/docker-compose.yml` on an offline machine
- CUDA and driver installation behavior is explicit for both bundled and missing-installer cases
</success_criteria>

<output>
After completion, create `.planning/phases/05-import-scripts/05-01-SUMMARY.md`
</output>
