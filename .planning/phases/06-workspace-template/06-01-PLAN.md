---
phase: 06-workspace-template
plan: 01
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - templates/workspace-template/.devcontainer/docker-compose.yml
  - templates/workspace-template/.devcontainer/devcontainer.json
  - templates/workspace-template/.devcontainer/configure-ollama-endpoint.sh
  - templates/workspace-template/.env.example
  - tests/container/test_workspace_template.sh
  - tests/container/run_all.sh
  - README.md
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
must_haves:
  truths:
    - "The template is copyable into another repo without required edits for the default compose-first path"
    - "The template mirrors the existing devcontainer and compose contract: dev-env, ollama, ai-net, and /workspace"
    - "Inline comments inside the template files explain the full air-gap operator flow from cuda-prep through reopen-in-container"
    - "Image references stay override-friendly while still showing explicit GHCR pinning examples"
    - "The active template remains CPU-safe and compose-internal by default; host fallback and GPU usage are documented as optional examples"
  artifacts:
    - path: "templates/workspace-template/.devcontainer/docker-compose.yml"
      provides: "Copyable compose template for new projects"
      contains: "OLLAMA_HOST"
    - path: "templates/workspace-template/.devcontainer/devcontainer.json"
      provides: "Copyable VS Code devcontainer template"
      contains: "runServices"
---

<objective>
Create a copyable workspace template that packages the repo's compose-backed devcontainer flow into a standalone onboarding artifact with inline workflow documentation for air-gapped deployment.

Output:
- `templates/workspace-template/` with the template `.devcontainer/` assets and any minimal helper files needed for copy/paste use
- Focused validation coverage that keeps the template aligned with the production compose/devcontainer contract
- Any minimal README touch-up needed so the repo points users at the template artifact clearly
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-workspace-template/06-CONTEXT.md
@.planning/phases/06-workspace-template/06-RESEARCH.md
@.planning/phases/05-import-scripts/05-01-SUMMARY.md

<interfaces>
D-01/D-02: The template lives under `templates/workspace-template/` and preserves the current compose-first architecture (`dev-env`, `ollama`, `ai-net`, `/workspace`).
D-03/D-04: Image references remain env-driven for portability, with explicit GHCR pinning examples documented inline.
D-06/D-07: Inline comments in the template files explain the full operator workflow from CUDA prep through export/import and VS Code reopen.
D-08/D-09: The active default path uses compose-internal Ollama and CPU-safe settings; host fallback and GPU usage appear only as clearly labeled examples.
D-10: A new project should be able to copy the template and use the default path without editing the files first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the copyable workspace template</name>
  <files>
    templates/workspace-template/.devcontainer/docker-compose.yml
    templates/workspace-template/.devcontainer/devcontainer.json
    templates/workspace-template/.devcontainer/configure-ollama-endpoint.sh
    templates/workspace-template/.env.example
    README.md
  </files>
  <action>
    1. Create `templates/workspace-template/` and mirror the existing compose/devcontainer contract into that directory.
    2. Keep the active defaults aligned with the current compose-first workflow: `dev-env`, `ollama`, `ai-net`, `/workspace`, and `OLLAMA_HOST=http://ollama:11434`.
    3. Add concise inline comments that document the operator flow end-to-end: optional `cuda-prep`, export on the staging machine, transfer, import on the offline machine, `docker compose up`, and VS Code reopen-in-container.
    4. Show GHCR pinning, Windows host Ollama fallback, and GPU usage as commented examples without making them the default runnable path.
    5. Add only the minimum helper file(s) needed to make overrides discoverable and copy/paste-friendly.
  </action>
  <verify>
    <automated>
      test -f templates/workspace-template/.devcontainer/docker-compose.yml
      test -f templates/workspace-template/.devcontainer/devcontainer.json
      test -f templates/workspace-template/.devcontainer/configure-ollama-endpoint.sh
      python3 -c "import json; d=json.load(open('templates/workspace-template/.devcontainer/devcontainer.json')); assert d['service']=='dev-env'; assert d['workspaceFolder']=='/workspace'; assert d['remoteUser']=='dev'; assert d['runServices']==['dev-env','ollama']"
      grep -q 'http://ollama:11434' templates/workspace-template/.devcontainer/docker-compose.yml
      grep -q '10.10.10.10:11434' templates/workspace-template/.devcontainer/docker-compose.yml
      grep -q 'cuda-prep' templates/workspace-template/.devcontainer/docker-compose.yml
      grep -q 'image-import' templates/workspace-template/.devcontainer/docker-compose.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - The template directory can be copied into another repo without requiring edits for the default path
    - The template compose/devcontainer files preserve the existing service names, workspace path, and endpoint defaults
    - The inline comments explicitly cover export, transfer, import, compose startup, and VS Code reopen
    - GHCR pinning, host fallback, and GPU usage are documented as optional examples rather than active defaults
  </acceptance_criteria>
  <done>
    The repo now contains a standalone workspace template that mirrors the production stack and documents the full air-gap workflow where operators need it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add lightweight drift checks for the template contract</name>
  <files>
    tests/container/test_workspace_template.sh
    tests/container/run_all.sh
    README.md
  </files>
  <action>
    1. Add a focused static test that verifies the template retains the required service names, workspace mount path, `OLLAMA_HOST` default, and inline workflow references.
    2. Wire that test into the existing container test runner so future changes to `.devcontainer/` or the template surface drift quickly.
    3. Update the README only as needed so readers know the template exists and how it relates to the existing compose/devcontainer assets.
  </action>
  <verify>
    <automated>
      test -f tests/container/test_workspace_template.sh
      grep -q 'workspace-template' tests/container/test_workspace_template.sh
      grep -q 'test_workspace_template.sh' tests/container/run_all.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - The repo has automated coverage for the template's core contract
    - The template is discoverable from the repo docs without requiring a separate template README
  </acceptance_criteria>
  <done>
    Template drift is now guarded by a focused static test and the artifact is discoverable from the repo documentation.
  </done>
</task>

</tasks>

<success_criteria>
- A standalone `templates/workspace-template/` artifact exists with the copyable `.devcontainer/` flow
- The template's inline comments cover the full air-gap workflow end-to-end
- The template remains aligned with the current compose/devcontainer contract and override strategy
- The repo includes lightweight validation to catch template drift over time
</success_criteria>

<output>
After completion, create `.planning/phases/06-workspace-template/06-01-SUMMARY.md`
</output>
