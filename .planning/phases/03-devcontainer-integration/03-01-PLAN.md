---
phase: 03-devcontainer-integration
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - .devcontainer/devcontainer.json
  - tests/container/test_configs.sh
  - README.md
autonomous: true
requirements:
  - DEV-01
  - DEV-02
  - DEV-03
must_haves:
  truths:
    - "devcontainer.json uses docker compose mode against .devcontainer/docker-compose.yml"
    - "VS Code attaches to the dev-env service, not the ollama sidecar"
    - "The workspace remains mounted at /workspace and the remote user remains dev"
    - "Both dev-env and ollama start when the devcontainer opens"
    - "The compose path and workspace mount do not drift from the existing Phase 2 stack"
  artifacts:
    - path: ".devcontainer/devcontainer.json"
      provides: "Compose-backed devcontainer entrypoint for VS Code"
      contains: "dockerComposeFile"
---

<objective>
Update `.devcontainer/devcontainer.json` so VS Code Reopen in Container starts the existing compose stack,
attaches to `dev-env`, keeps the workspace mounted at `/workspace`, and starts both `dev-env` and `ollama`.

Output:
- Updated `.devcontainer/devcontainer.json`
- Any minimal test/doc updates needed to keep the devcontainer contract explicit and verifiable
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-devcontainer-integration/03-CONTEXT.md
@.planning/phases/02-compose-stack/02-CONTEXT.md
@.planning/phases/02-compose-stack/02-01-SUMMARY.md

<interfaces>
D-01: `dockerComposeFile` points to `docker-compose.yml` relative to `.devcontainer/`.
D-02: `service` is `dev-env`; `runServices` includes `dev-env` and `ollama`.
D-03: `workspaceFolder` remains `/workspace`.
D-04: `workspaceMount` remains explicitly bound to `/workspace`.
D-05: `remoteUser` remains `dev`.
D-06: Do not change compose service names or paths in this phase; devcontainer.json must adapt to Phase 2, not redefine it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch devcontainer.json to compose mode</name>
  <files>
    .devcontainer/devcontainer.json
    tests/container/test_configs.sh
    README.md
  </files>
  <action>
    1. Replace the image-based `.devcontainer/devcontainer.json` config with compose-backed configuration referencing `docker-compose.yml`.
    2. Set `service` to `dev-env`, preserve `workspaceFolder: /workspace`, preserve `workspaceMount`, and preserve `remoteUser: dev`.
    3. Add `runServices` with both `dev-env` and `ollama` so VS Code starts the sidecar automatically.
    4. Update only the minimum surrounding verification/docs needed so the repo clearly expects compose-backed devcontainer startup.
  </action>
  <verify>
    <automated>
      test -f .devcontainer/devcontainer.json
      python3 -c "import json; d=json.load(open('.devcontainer/devcontainer.json')); assert d['dockerComposeFile'] in ('docker-compose.yml', ['docker-compose.yml']); assert d['service']=='dev-env'; assert d['workspaceFolder']=='/workspace'; assert d['workspaceMount'].startswith('source=${localWorkspaceFolder},target=/workspace,type=bind'); assert d['remoteUser']=='dev'; assert d['runServices']==['dev-env','ollama']"
      grep -q 'dockerComposeFile' .devcontainer/devcontainer.json
      grep -q '"service": "dev-env"' .devcontainer/devcontainer.json
      grep -q '"runServices": \["dev-env", "ollama"\]' .devcontainer/devcontainer.json || grep -q '"runServices": \[' .devcontainer/devcontainer.json
      if command -v devcontainer >/dev/null 2>&1; then devcontainer read-configuration --workspace-folder . >/dev/null; else echo "WARN: devcontainer CLI unavailable locally; skipped read-configuration check"; fi
    </automated>
  </verify>
  <acceptance_criteria>
    - `.devcontainer/devcontainer.json` is valid JSON
    - `dockerComposeFile` references `.devcontainer/docker-compose.yml` via the correct relative path from `.devcontainer/`
    - `service` is `dev-env`
    - `runServices` contains both `dev-env` and `ollama`
    - `workspaceFolder` is `/workspace`
    - `workspaceMount` still targets `/workspace`
    - `remoteUser` is `dev`
  </acceptance_criteria>
  <done>
    VS Code devcontainer startup now routes through the compose stack, attaches to `dev-env`, and starts both `dev-env` and `ollama` while preserving the existing workspace and user contract.
  </done>
</task>

</tasks>

<success_criteria>
- `python3 -c "import json; json.load(open('.devcontainer/devcontainer.json'))"` exits 0
- `devcontainer.json` references compose mode instead of a direct image
- `runServices` starts both required services
- The workspace path and remote user still match the container image contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-devcontainer-integration/03-01-SUMMARY.md`
</output>
