---
phase: quick
plan: 260410-ms8
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - tests/container/test_workspace_template.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "pi binary resolves Node.js ESM imports correctly from the package directory"
    - "test_workspace_template.sh works in both local (devcontainer) and CI environments"
  artifacts:
    - path: "Dockerfile"
      provides: "ln -sf symlink for pi instead of COPY-dereferenced plain file"
      contains: "ln -sf /home/dev/.local/lib/node_modules/@mariozechner/pi-coding-agent/dist/cli.js"
    - path: "tests/container/test_workspace_template.sh"
      provides: "TEMPLATE_DIR driven by TEMPLATES_DIR env var with sensible default"
      contains: "TEMPLATES_DIR:-/workspace/templates"
  key_links:
    - from: "/home/dev/.local/bin/pi"
      to: "/home/dev/.local/lib/node_modules/@mariozechner/pi-coding-agent/dist/cli.js"
      via: "symbolic link created by RUN ln -sf"
    - from: "test_workspace_template.sh TEMPLATE_DIR"
      to: "TEMPLATES_DIR env var"
      via: "bash parameter expansion with default"
---

<objective>
Fix two bugs introduced by Docker's symlink-dereference behavior and a hardcoded CI path.

Purpose: Restore pi binary functionality in the final image stage, and make the workspace
template test portable across local devcontainer and CI environments.

Output: Updated Dockerfile (ln -sf instead of COPY for pi bin) and updated
test_workspace_template.sh (TEMPLATES_DIR env var with /workspace/templates default).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace COPY of pi bin with ln -sf symlink in Dockerfile</name>
  <files>Dockerfile</files>
  <action>
Remove line 270:
  COPY --chown=dev:dev --from=builder-ai-tools /home/dev/.local/bin/pi /home/dev/.local/bin/pi

Replace it with a RUN directive immediately after the @mariozechner package COPY (line 271).
The replacement must be:
  RUN ln -sf /home/dev/.local/lib/node_modules/@mariozechner/pi-coding-agent/dist/cli.js /home/dev/.local/bin/pi

No other Dockerfile lines should change. The ln -sf must follow the COPY of
/home/dev/.local/lib/node_modules/@mariozechner so the target exists when the symlink
is created.
  </action>
  <verify>
grep -n "ln -sf.*pi-coding-agent" Dockerfile
grep -n "COPY.*bin/pi" Dockerfile  # must return nothing
  </verify>
  <done>
Dockerfile contains the RUN ln -sf line pointing to pi-coding-agent/dist/cli.js and the
COPY --from=builder-ai-tools of /home/dev/.local/bin/pi is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make TEMPLATE_DIR in test_workspace_template.sh env-var-driven</name>
  <files>tests/container/test_workspace_template.sh</files>
  <action>
Change line 7 from:
  TEMPLATE_DIR="/templates/workspace-template"

To:
  TEMPLATE_DIR="${TEMPLATES_DIR:-/workspace/templates}/workspace-template"

No other lines in the file should change. This preserves CI behavior when the CI docker
run passes -e TEMPLATES_DIR=/templates, and makes local devcontainer runs work with the
/workspace path without any extra configuration.
  </action>
  <verify>
grep -n 'TEMPLATES_DIR:-/workspace/templates' tests/container/test_workspace_template.sh
  </verify>
  <done>
Line 7 of test_workspace_template.sh reads:
  TEMPLATE_DIR="${TEMPLATES_DIR:-/workspace/templates}/workspace-template"
All other lines are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder-ai-tools → final image | Symlink target path must exist before ln -sf runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ms8-01 | Tampering | pi symlink target path | accept | Path is baked from a controlled builder stage; no external input |
| T-ms8-02 | Elevation of Privilege | TEMPLATES_DIR env var | accept | Env var only affects test path resolution; no privilege boundary crossed |
</threat_model>

<verification>
After both edits:
1. `grep -n "ln -sf.*pi-coding-agent" Dockerfile` returns the new RUN line
2. `grep -n "COPY.*bin/pi" Dockerfile` returns no output
3. `grep -n 'TEMPLATES_DIR:-/workspace/templates' tests/container/test_workspace_template.sh` returns line 7
4. `bash -n tests/container/test_workspace_template.sh` exits 0 (syntax check)
</verification>

<success_criteria>
- Dockerfile: pi bin is a real symlink in the final image (Node.js ESM resolves imports from
  the package directory, not from ~/.local/bin/)
- test_workspace_template.sh: TEMPLATE_DIR defaults to /workspace/templates/workspace-template
  locally, and CI can override via -e TEMPLATES_DIR=/templates with no CI workflow changes
</success_criteria>

<output>
After completion, create .planning/quick/260410-ms8-fix-pi-symlink-and-test-workspace-templa/260410-ms8-SUMMARY.md
</output>
