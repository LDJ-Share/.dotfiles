---
phase: 04-export-scripts-cuda-prep
plan: 01
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - image-export.sh
  - image-export.ps1
  - cuda-prep.sh
  - cuda-prep.ps1
  - tests/container/test_export_scripts.sh
  - README.md
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03
  - EXPORT-04
  - CUDA-01
  - CUDA-02
  - CUDA-03
must_haves:
  truths:
    - "Export creates one gzipped archive covering the compose image set"
    - "Export produces SHA256 verification data and a manifest.json with image and installer metadata"
    - "CUDA prep downloads into a predictable staging directory that export can bundle"
    - "Bash and PowerShell produce the same archive structure"
    - "Scripts document the exact offline-machine commands needed to collect GPU, driver, kernel, and OS details"
  artifacts:
    - path: "image-export.sh"
      provides: "Linux/WSL export workflow"
      contains: "manifest.json"
    - path: "cuda-prep.sh"
      provides: "Linux/WSL CUDA prep workflow"
      contains: "nvidia"
---

<objective>
Create the first export-side transport workflow so a connected staging machine can package the current compose images and any prepared CUDA artifacts into a single transfer archive usable by the offline import step.

Output:
- `image-export.sh`
- `image-export.ps1`
- `cuda-prep.sh`
- `cuda-prep.ps1`
- Minimal tests/docs needed to keep the archive contract explicit and verifiable
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-export-scripts-cuda-prep/04-CONTEXT.md
@.planning/phases/03-devcontainer-integration/03-UAT.md

<interfaces>
D-01/D-02: Export produces a single `.tar.gz` plus checksum artifact.
D-03: `manifest.json` records image names, tags, digests when available, archive metadata, and bundled installer paths.
D-04/D-05: The script must validate Docker availability and local image presence before export.
D-06/D-07: CUDA prep downloads artifacts based on supplied target-machine details and includes inline discovery commands for the offline machine.
D-08/D-10: CPU-only systems must remain supported; the workflow cannot require GHCR publication to succeed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the archive export contract</name>
  <files>
    image-export.sh
    image-export.ps1
    tests/container/test_export_scripts.sh
    README.md
  </files>
  <action>
    1. Implement Bash and PowerShell export scripts that package the current compose image set into one gzipped tar archive.
    2. Generate `SHA256SUMS` and `manifest.json` alongside the archive, capturing image references, tags, archive name, checksum, and bundled installer metadata.
    3. Make the export path work with the repo's current local-compose defaults and allow image overrides where that reduces coupling.
    4. Add minimal verification coverage for script usage/help text and manifest/checksum contract.
  </action>
  <verify>
    <automated>
      test -f image-export.sh
      test -f image-export.ps1
      grep -q 'manifest.json' image-export.sh
      grep -q 'SHA256' image-export.ps1
      test -f tests/container/test_export_scripts.sh
    </automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build CUDA prep staging for export bundling</name>
  <files>
    cuda-prep.sh
    cuda-prep.ps1
    README.md
  </files>
  <action>
    1. Implement Bash and PowerShell CUDA prep scripts that accept target GPU/driver/OS inputs and download the matching Linux toolkit/container-runtime artifacts plus the Windows driver installer.
    2. Stage those downloads in a predictable directory layout that `image-export` can include unchanged.
    3. Include inline comments showing the exact offline-machine commands to gather GPU name, driver version, kernel version, and OS release.
  </action>
  <verify>
    <automated>
      test -f cuda-prep.sh
      test -f cuda-prep.ps1
      grep -q 'nvidia-smi --query-gpu=name --format=csv,noheader' cuda-prep.sh
      grep -q 'lsb_release -rs' cuda-prep.sh
    </automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Export scripts exist for Bash and PowerShell and describe the same archive contract
- The repo has a clear manifest/checksum format for Phase 5 import to consume
- CUDA prep outputs are staged in a location the export scripts can bundle without extra translation
- The workflow remains usable with the current local image defaults while GHCR publication is blocked
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-scripts-cuda-prep/04-01-SUMMARY.md`
</output>
